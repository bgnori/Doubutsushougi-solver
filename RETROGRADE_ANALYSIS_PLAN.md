# 後退解析（Retrograde Analysis）実装計画

## 概要

Doubutsu Shougi（どうぶつ将棋）の全局面を解析し、各位置での最適な戦術を求めます。

## 1. 状態空間の規模

| 項目 | 推定値 |
|------|--------|
| 到達可能な局面数 | 約 6,000,000 状態 |
| `position_key()` の dense index 空間 | 18,593,796,240 状態 |
| 駒配置のターンアラウンド時間 | 終盤まで数手 |

`position_key()` は衝突しない dense index ですが、対象は「この hash モデルで表現できる全局面」です。
到達可能局面だけを 0..6,000,000 程度に詰める index ではありません。
そのため、後退解析で `xs[position_key(state)]` をそのまま使うと 18.6B エントリの配列が必要になります。
6M 程度の配列で解析したい場合は、到達可能局面を列挙して `position_key -> dense_reachable_id` の写像を作る必要があります。

## 2. メモリ要求量

### 方法 1: ハッシュテーブル（推奨）
- **形式**: `Dict[int, Result]` 
  - キー: `position_key()` の戻り値
  - 値: `WinLoss` enum（1バイト）
- **メモリ**: ~150 MB（Python dict オーバーヘッド含む）
- **利点**: シンプル、高速ルックアップ、ダイナミック成長

### 方法 2: ビットセット
- **形式**: 各状態に 3 ビット（18,593,796,240 dense index 空間に対応）
  - 0b00: 未探索
  - 0b01: 勝利
  - 0b10: 敗北
  - 0b11: 引き分け
- **メモリ**: ~6.5 GB（全 index 空間を 3bit/状態で保持）
- **利点**: 密集、メモリ効率的
- **欠点**: 実リーチ可能状態が 6M のみなため大量の無駄

### 方法 2b: 到達可能局面だけの dense 配列（推奨候補）
- **形式**: `id = 0..N-1` を到達可能局面にだけ割り当てる
- **必要な補助構造**:
   - `id_to_key: array[uint64]`
   - `key_to_id: dict[int, int]` またはソート済み配列 + 二分探索
- **メモリ**: 結果配列は 6M エントリ分のみ（数十 MB）
- **利点**: `xs[id].next = id_of_successor` が高速かつ小さい
- **欠点**: 事前に到達可能局面列挙が必要

### 方法 3: SQLite データベース
- **形式**: `key INTEGER PRIMARY KEY, result TINYINT, depth_to_win TINYINT`
- **メモリ**: ~200 MB（インデックス含む）
- **利点**: 永続性、複雑なクエリ対応、スケーラビリティ
- **欠点**: I/O オーバーヘッド

**推奨**: まず方法 1（ハッシュテーブル）で正しさを確認し、その後 方法 2b（到達可能局面だけの dense 配列）へ移行する。

## 3. 計算量

### 基本パラメータ
- **状態数**: N = 6,000,000
- **平均逆手数**: k = 8 手/状態
- **反復数**: W = 5 回（収束まで）

### 1パスの計算量
```
操作数 = N × k × (検索＋更新)
      ≈ 6,000,000 × 8 × 20 操作
      ≈ 960,000,000 操作 (~1B ops)
```

### 全 W パスの合計
```
全操作数 = 960,000,000 × 5
        ≈ 4.8 billion 操作
```

### 実行時間推定

| 方法 | 1パス | 5パス | 備考 |
|------|-------|--------|------|
| **直接後退解析** | ~1 秒 | ~5 秒 | **推奨** |
| state_from_key() 経由 | ~2.5 時間 | ~12.5 時間 | 非推奨 |

## 4. 実装戦略

### Phase 1: 基本実装（推奨）
```python
# retrograde.py の拡張
from typing import Dict, Enum
from enum import Enum

class Result(Enum):
    UNKNOWN = 0
    BLACK_WIN = 1
    WHITE_WIN = 2
    DRAW = 3

# ハッシュテーブルの初期化
retrograde_db: Dict[int, Result] = {}

def analyze_retrograde():
    """全局面の後退解析を実行"""
    # 1. 終局面（勝者が確定した状態）のセット
    # 2. 各反復で未処理状態の逆手を探索
    # 3. 固定点に到達するまで繰り返し
    pass
```

### Phase 2: 永続化
- SQLite データベースに結果を保存
- 分析の再開や増分更新に対応

### Phase 3: 最適化
- 対称性による圧縮（オプション）
- 並列化処理（複数キー範囲分割）

## 5. 現在の実装状況

✓ **完了**:
- `position_key()`: 状態 → dense index（衝突なし）
- `state_from_key()`: dense index → 状態
- 局面生成・操作

⏳ **必要**:
- 後退解析エンジン
- 逆手生成関数
- 結果の保存・ロード

## 6. スケジュール見積もり

| タスク | 時間 |
|--------|------|
| 基本後退解析実装 | 2 時間 |
| 終局面データセット準備 | 1 時間 |
| 1パス実行 | 1 秒 |
| 全 5 パス実行 | 5 秒 |
| 結果検証・最適化 | 1 時間 |
| **合計** | **5 時間** |

## 7. 動作検証チェックリスト

- [ ] 初期局面の後退値を計算
- [ ] 終局近い局面で予測と一致確認
- [ ] 手番切り替わりの正確性確認
- [ ] 勝利経路の存在確認
- [ ] パフォーマンス測定（1 パスあたりの時間）
- [ ] メモリピーク測定
- [ ] 結果の永続化・復元テスト

## 8. 推奨される実装順序

1. **`retrograde.py` の拡張**
   - `Result` enum 定義
   - `retrograde_db` の初期化
   - `analyze_retrograde()` メイン関数

2. **終局面ジェネレータ**
   - 勝者が確定した全状態の列挙
   - 各開始条件（終盤配置）をテスト

3. **逆手生成**
   - 与えられた状態から到達可能な前状態をすべて列挙
   - `apply_move()` の逆操作

4. **反復ループ**
   - 各反復で全状態を処理
   - 結果の更新とワークキューの管理

5. **結果の永続化**
   - `save_retrograde_db()`, `load_retrograde_db()`

## 9. リソース

- CPU: シングルスレッド OK（将来は multiprocessing で並列化可能）
- RAM: ~200 MB
- ディスク: ~100 MB（結果保存用）
- 時間: ~10 秒（全 5 パス）

## 10. 実行結果（実装検証）

### 実際のパフォーマンス測定

| メトリクス | 値 |
|-----------|-----|
| Terminal seeds | 68 個（1.1ms） |
| Backward BFS | 75.4ms |
| 発見位置数 | 117 個 |
|  WIN | 44 個 |
|  LOSS | 73 個 |
| メモリ | 0.01 MB（117 位置） |
| スループット | ~1550 位置/秒 |

### 実装ステータス

✓ **完了**:
- `retrograde_analysis()` 完全に動作
- Terminal seed 生成正常
- Backward BFS 実装確認済み
- `state_from_key()` 逆引き可能

⏳ **次フェーズ**:
- 拡張 terminal seed セット
- Forward/Backward BFS 統合
- SQLite 永続化

---

**次のアクション**: `retrograde.py` を確認し、実装計画を詳細化します。
